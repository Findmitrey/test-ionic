import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.testionic.fin.dmitry',
  appName: 'test-ionic',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
